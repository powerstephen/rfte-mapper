"use client";

export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>R/FTE Mapper — smoke test ✅</h1>
      <p>If you can see this on Vercel, the build is fixed. Next we re-add the UI.</p>
    </main>
  );
}
